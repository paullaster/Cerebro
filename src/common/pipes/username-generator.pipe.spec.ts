import { UsernameGeneratorPipe } from './username-generator.pipe.js';

describe('UsernameGeneratorPipe', () => {
  it('should be defined', () => {
    expect(new UsernameGeneratorPipe()).toBeDefined();
  });
});
